# MQTT_Application
Demonstrate a MQTT server and connection setting, sending message from publisher and receive from subcriber.

## Note
This application is using MQTTX platform

- Open `Publisher.py` , install the necessary libraries,  Run the code, 
- Following shows the connection status ```Type a value and press Enter (type 'exit' to quit):
> ✅ Connected to broker via WebSocket (ws://)```
- Type/ Input the value, number, e.g. ``hello world~!```
- The terminal will show the content just published, `📤 Published: hello world~!`

